use alloc::string::String;
use alloc::vec::Vec;
use cosmrs::crypto::PublicKey;
// use cosmrs::tx::SignatureBytes;

pub fn verify_signature(_publik_key: PublicKey, _sign_bytes: Vec<u8>) -> Result<(), String> {
    todo!()
}
